<form method="post" action="{{ path('app_decortiqueur_plan_deco_delete', {'id': plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plan.id) }}">
    <button class="btn">Delete</button>
</form>
